#flask==3.0.0 #A lightweight Python web framework ,Creating API endpoints (/health, /event, etc.)Handling HTTP requests and responses
#psycopg2-binary==2.9.9 #PostgreSQL database adapter for Python ✅ Why you need it: It allows your app to: Connect to PostgreSQL,Execute SQL queries ,Insert and fetch data,In production, people often prefer psycopg2 (non-binary) for better control
#prometheus-client==0.19.0 #Official Prometheus library for Python,Create metrics,Expose metrics endpoint 

#YOUR API STACK:-
# Layer	        Tool Used	  Purpose
# Web framework	Flask	          API handling
# Database	psycopg2	  PostgreSQL access
# Monitoring	prometheus-client Metrics tracking

# 🧠 Summary
# Your requirements.txt enables your application to:
# ✔ Serve APIs (Flask)
# ✔ Store events in PostgreSQL (psycopg2)
# ✔ Export metrics for monitoring (Prometheus)


#opentelemetry-api==1.21.0
#opentelemetry-sdk==1.21.0
#opentelemetry-instrumentation-flask==0.42b0
#opentelemetry-instrumentation-psycopg2==0.42b0
#opentelemetry-exporter-otlp-proto-http==1.21.0


#protobuf>=6.33.5
#wheel>=0.46.2
flask==3.0.0
psycopg2-binary==2.9.9
prometheus-client==0.19.0
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-flask==0.42b0
opentelemetry-instrumentation-psycopg2==0.42b0
opentelemetry-exporter-otlp-proto-http==1.21.0
protobuf>=3.19,<5.0
wheel>=0.46.2
requests==2.31.0